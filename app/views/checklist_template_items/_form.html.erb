<%= bootstrap_form_with(model: checklist_template_item) do |form| %>
  <% if checklist_template_item.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(checklist_template_item.errors.count, "error") %> prohibited this checklist_template_item from being saved:</h2>

      <ul>
        <% checklist_template_item.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.collection_select(:checklist_template_id, ChecklistTemplate.all, :id, :name) %>
  </div>

  <div>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.text_area :description %>
  </div>

  <div>
    <%= form.number_field :expected_duration_days %>
  </div>

  <div>
    <%= form.check_box :required %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
