Congrats on <%= @pet.name %>'s adoption!

<% if @assignments.one? %>
Hope everything's going well! This is just a reminder on a task for <%= @pet.name %>.

<%= render partial: 'match_mailer/checklist_assignment', locals: { assignment: @assignments.first } %>

<% else %>
This is just a reminder on tasks for <%= @pet.name %>.

<% @assignments.each do |assignment| %>
<%= render partial: 'match_mailer/checklist_assignment', locals: { assignment: assignment } %>
<% end %>
<% end %>

Have a great day!
