<div class="d-flex flex-column align-items-center mb-4">
  <iframe id="external-form" 
    src=<%= @form_url%>
    width="640" 
    height="645"
    >Loading…</iframe>

  <%= link_to 'View pets', adoptable_pets_path, class: "my-2" %>
</div>