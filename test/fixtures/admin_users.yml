# Read about fixtures at https://api.rubyonrails.org/classes/ActiveRecord/FixtureSet.html

# This model initially had no columns defined. If you add columns to the
# model remove the "{}" from the fixture names and add the columns immediately
# below each fixture, per the syntax in the comments below
#

# These fixtures commented out as they are causing test errors, where both users
# have the same email (none) and its causing a unique constraint issue
# see https://stackoverflow.com/questions/8443741/mysql2error-duplicate-entry-for-key-index-admin-users-on-email-ruby-on-r
# one: {}
#  column: value
# 
# two: {}
#   column: value
